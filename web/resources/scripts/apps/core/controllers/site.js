// == Application =========================

jQuery( document ).ready( function() {

	// Access App
	var app	= cmt.api.root.getApplication( 'core' );
});

// == Site Controller =====================

/*
cmg.core.controllers.SiteController.prototype.testActionPre = function( requestElement ) {

	return true;
};

cmg.core.controllers.SiteController.prototype.testActionFailure = function( requestElement, response ) {

};

cmg.core.controllers.SiteController.prototype.testActionSuccess = function( requestElement, response ) {

}
*/

// == Direct Calls ========================

// == Additional Methods ==================
